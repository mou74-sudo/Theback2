/* app.js — router, state, all 6 screens, all visualisers */
const STATE={route:"home",routeArg:null,history:[],flashIndex:0,flashSet:null,bigOIndex:0,bigOScore:0,bigOAnswered:false,bigOOrder:null,streak:0};
const STORE={load(){try{return JSON.parse(localStorage.getItem("dsa_app"))||{};}catch{return{};}},save(d){localStorage.setItem("dsa_app",JSON.stringify(d));},get(k,f){const d=this.load();return k in d?d[k]:f;},set(k,v){const d=this.load();d[k]=v;this.save(d);}};
function applyTheme(){const t=STORE.get("theme","dark");document.documentElement.dataset.theme=t;document.getElementById("theme-btn").textContent=t==="dark"?"☼":"☾";}
document.getElementById("theme-btn").addEventListener("click",()=>{STORE.set("theme",STORE.get("theme","dark")==="dark"?"light":"dark");applyTheme();});
applyTheme();
(function tickStreak(){const today=new Date().toISOString().slice(0,10);const last=STORE.get("lastVisit",null);let s=STORE.get("streak",0);if(last!==today){const y=new Date(Date.now()-86400000).toISOString().slice(0,10);s=last===y?s+1:1;STORE.set("streak",s);STORE.set("lastVisit",today);}STATE.streak=s;})();
const SCREENS={};
function go(route,arg){if(STATE.route!==route||STATE.routeArg!==arg){STATE.history.push({route:STATE.route,arg:STATE.routeArg});if(STATE.history.length>30)STATE.history.shift();}STATE.route=route;STATE.routeArg=arg;render();}
function back(){const p=STATE.history.pop();if(p){STATE.route=p.route;STATE.routeArg=p.arg;render();}else go("home");}
document.getElementById("back-btn").addEventListener("click",back);
document.querySelectorAll(".tabbar .tab").forEach(t=>t.addEventListener("click",()=>{STATE.history=[];go(t.dataset.route);}));
const TITLES={home:{t:"DSA Study",s:"COMP5066 · Exam revision"},flashcards:{t:"Flashcards",s:"Q1-style concept review"},bigO:{t:"Big-O Quiz",s:"Q2-style complexity practice"},sort:{t:"Sort Visualiser",s:"Q3-style step-by-step"},bst:{t:"BST Builder",s:"Q4 · same format, different numbers"},graph:{t:"Graph Traversal",s:"Q5 · BFS / DFS practice"},lecture:{t:"Lecture",s:"Notes & key points"}};
function render(){const r=STATE.route;const m=TITLES[r]||TITLES.home;document.getElementById("screen-title").textContent=m.t;document.getElementById("screen-subtitle").textContent=m.s;document.getElementById("back-btn").hidden=(r==="home");document.querySelectorAll(".tabbar .tab").forEach(t=>t.classList.toggle("active",t.dataset.route===r));const app=document.getElementById("app");app.innerHTML="";(SCREENS[r]||SCREENS.home)(app,STATE.routeArg);window.scrollTo({top:0,behavior:"instant"});}
function el(tag,attrs={},...children){const node=document.createElement(tag);for(const[k,v]of Object.entries(attrs)){if(k==="class")node.className=v;else if(k==="html")node.innerHTML=v;else if(k.startsWith("on")&&typeof v==="function")node.addEventListener(k.slice(2),v);else if(k==="style"&&typeof v==="object")Object.assign(node.style,v);else if(v===true)node.setAttribute(k,"");else if(v!==false&&v!=null)node.setAttribute(k,v);}for(const c of children){if(c==null||c===false)continue;node.appendChild(typeof c==="string"?document.createTextNode(c):c);}return node;}
function toast(msg,ms=1600){const t=el("div",{class:"toast"},msg);document.body.appendChild(t);setTimeout(()=>t.remove(),ms);}
function shuffle(arr){const a=arr.slice();for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]];}return a;}
function daysUntil(){const target=new Date("2026-05-26T14:00:00");return Math.ceil((target-new Date())/(24*3600*1000));}

SCREENS.home=function(root){
  const days=daysUntil();const daysLabel=days>=0?`${days} days to exam`:`Exam was ${-days} days ago`;
  root.appendChild(el("section",{class:"hero"},el("h2",{},"Smash the DSA exam"),el("p",{},EXAM_INFO.date+" · "+EXAM_INFO.time+" · "+EXAM_INFO.location.split(",")[0]),el("span",{class:"countdown"},"⏰ "+daysLabel)," ",STATE.streak>0?el("span",{class:"streak"},"🔥 "+STATE.streak+"-day streak"):null));
  const qWrap=el("section",{class:"card"},el("h2",{},"🎯 The exam · 5 questions"),el("p",{class:"muted",style:{marginBottom:"8px"}},EXAM_INFO.format));
  EXAM_INFO.questions.forEach(q=>{const row=el("div",{style:{padding:"8px 0",borderTop:"1px dashed var(--bg-3)"}},el("div",{class:"row-between"},el("strong",{},"Q"+q.n),q.note?el("span",{class:"tag-pill amber"},q.note):el("span",{class:"tag-pill"},q.topic)),el("p",{style:{fontSize:"13px",margin:"4px 0 0",color:"var(--text-dim)"}},q.desc));qWrap.appendChild(row);});
  root.appendChild(qWrap);
  root.appendChild(el("section",{class:"quick-grid"},quickTile("📝","Concept cards","Q1 — definitions","flashcards"),quickTile("🔬","Big-O quiz","Q2 — complexity","bigO"),quickTile("🔢","Sort visualiser","Q3 — sort by hand","sort"),quickTile("🌳","BST builder","Q4 — same as mock","bst"),quickTile("🕸️","Graph traversal","Q5 — same as mock","graph"),quickTile("📚","Lectures","All 12 topics","lectures")));
  const grid=el("section",{},el("h2",{style:{fontSize:"16px",margin:"14px 4px 8px"}},"📚 Lectures 1 – 12"));const gridWrap=el("div",{class:"lect-grid"});
  LECTURES.forEach(l=>{gridWrap.appendChild(el("button",{class:"lect-tile"+(l.examQ?" exam":""),onclick:()=>go("lecture",l.n)},el("span",{class:"num"},"LECTURE "+l.n),el("div",{class:"name"},l.title),el("span",{class:"tag"},l.tag)));});
  grid.appendChild(gridWrap);root.appendChild(grid);
  root.appendChild(el("p",{class:"muted",style:{textAlign:"center"}},"Built for Dr Cetinkaya's COMP5066 · Tap any tile to practise"));
};
function quickTile(icon,ttl,sub,route){return el("button",{class:"quick-tile",onclick:()=>go(route)},el("div",{class:"icon"},icon),el("div",{class:"ttl"},ttl),el("div",{class:"sub"},sub));}

SCREENS.lectures=function(root){document.getElementById("screen-title").textContent="Lectures";const grid=el("div",{class:"lect-grid"});LECTURES.forEach(l=>{grid.appendChild(el("button",{class:"lect-tile"+(l.examQ?" exam":""),onclick:()=>go("lecture",l.n)},el("span",{class:"num"},"LECTURE "+l.n),el("div",{class:"name"},l.title),el("span",{class:"tag"},l.tag)));});root.appendChild(grid);};

SCREENS.lecture=function(root,n){
  const l=LECTURES.find(x=>x.n===Number(n));if(!l){root.appendChild(el("p",{},"Lecture not found."));return;}
  document.getElementById("screen-title").textContent="L"+l.n+" · "+l.title;document.getElementById("screen-subtitle").textContent=l.tag;
  root.appendChild(el("section",{class:"card"},el("h2",{},"Lecture "+l.n+" — "+l.title),l.examQ?el("span",{class:"tag-pill amber"},"Mock exam Q"+l.examQ):null,el("p",{style:{marginTop:"8px"}},l.exam||"")));
  root.appendChild(el("section",{class:"card detail-section"},el("h3",{},"Outline"),el("ul",{},...l.outline.map(x=>el("li",{},x)))));
  root.appendChild(el("section",{class:"card detail-section"},el("h3",{},"Key points"),el("ul",{},...l.keypoints.map(x=>el("li",{},x)))));
  if(l.code)root.appendChild(el("section",{class:"card detail-section"},el("h3",{},"Code"),el("pre",{class:"code-block"},l.code)));
  const p=el("div",{class:"btn-row",style:{marginTop:"12px"}});
  if(l.examQ===4||l.n===6)p.appendChild(el("button",{class:"btn",onclick:()=>go("bst")},"🌳 Practise BST"));
  if(l.examQ===5||l.n===5)p.appendChild(el("button",{class:"btn",onclick:()=>go("graph")},"🕸️ Practise BFS / DFS"));
  if([2,3].includes(l.n))p.appendChild(el("button",{class:"btn",onclick:()=>go("sort")},"🔢 Sort visualiser"));
  p.appendChild(el("button",{class:"btn btn-secondary",onclick:()=>go("flashcards")},"📝 Flashcards"));
  root.appendChild(p);
};

SCREENS.flashcards=function(root){
  if(!STATE.flashSet)STATE.flashSet=shuffle(FLASHCARDS);
  let i=STATE.flashIndex;
  if(i>=STATE.flashSet.length){STATE.flashSet=shuffle(FLASHCARDS);STATE.flashIndex=i=0;}
  const card=STATE.flashSet[i];
  const filterRow=el("div",{class:"chip-row"});
  const topics=["all",...new Set(FLASHCARDS.map(c=>c.topic))];
  const curFilter=STATE.flashFilter||"all";
  topics.forEach(tp=>filterRow.appendChild(el("button",{class:"chip"+(curFilter===tp?" active":""),onclick:()=>{STATE.flashFilter=tp;STATE.flashSet=shuffle(tp==="all"?FLASHCARDS:FLASHCARDS.filter(c=>c.topic===tp));STATE.flashIndex=0;render();}},tp==="all"?"All topics":tp)));
  root.appendChild(filterRow);
  const flash=el("div",{class:"flashcard",onclick:()=>flash.classList.toggle("flipped")},el("div",{class:"flashcard-inner"},el("div",{class:"flash-face flash-front"},el("div",{class:"label"},card.tag),el("div",{class:"q"},card.q),el("div",{class:"label",style:{marginTop:"auto",fontStyle:"italic"}},"Tap to reveal →")),el("div",{class:"flash-face flash-back"},el("div",{class:"label"},"ANSWER"),el("div",{class:"a"},card.a))));
  root.appendChild(flash);
  root.appendChild(el("div",{class:"flashcard-meta"},el("span",{},`Card ${i+1} / ${STATE.flashSet.length}`),el("span",{class:"progress-pill"},card.tag)));
  root.appendChild(el("div",{class:"btn-row",style:{marginTop:"12px",justifyContent:"space-between"}},el("button",{class:"btn btn-secondary",onclick:()=>{STATE.flashIndex=Math.max(0,i-1);render();}},"← Prev"),el("button",{class:"btn btn-ghost",onclick:()=>{STATE.flashSet=shuffle(STATE.flashSet);STATE.flashIndex=0;toast("Shuffled");render();}},"🔀 Shuffle"),el("button",{class:"btn",onclick:()=>{STATE.flashIndex=i+1;render();}},"Next →")));
  root.appendChild(el("p",{class:"muted",style:{textAlign:"center",marginTop:"12px"}},"Q1 of the exam is 4 × 5-mark concept questions like these."));
};

SCREENS.bigO=function(root){
  if(!STATE.bigOOrder){STATE.bigOOrder=shuffle(BIGO_QUIZ.map((_,i)=>i));STATE.bigOIndex=0;STATE.bigOScore=0;STATE.bigOAnswered=false;}
  if(STATE.bigOIndex>=STATE.bigOOrder.length)return renderBigOResult(root);
  const q=BIGO_QUIZ[STATE.bigOOrder[STATE.bigOIndex]];
  root.appendChild(el("div",{class:"quiz-meta"},el("span",{},`Question ${STATE.bigOIndex+1} / ${STATE.bigOOrder.length}`),el("span",{class:"tag-pill green"},`Score: ${STATE.bigOScore}`)));
  root.appendChild(el("h2",{style:{fontSize:"16px",margin:"6px 0 8px"}},"What is the time complexity?"));
  root.appendChild(el("pre",{class:"quiz-stem"},q.code));
  const opts=el("div",{class:"quiz-options"});
  q.options.forEach((opt,idx)=>{const btn=el("button",{class:"quiz-opt",onclick:()=>{if(STATE.bigOAnswered)return;STATE.bigOAnswered=true;Array.from(opts.children).forEach(c=>c.classList.add("disabled"));if(idx===q.answer){btn.classList.add("correct");STATE.bigOScore++;toast("✓ Correct");}else{btn.classList.add("wrong");opts.children[q.answer].classList.add("correct");toast("✗ Not quite");}root.appendChild(el("div",{class:"card",style:{marginTop:"12px"}},el("h3",{},idx===q.answer?"Right!":"Explanation"),el("p",{},q.why)));root.appendChild(el("button",{class:"btn",style:{marginTop:"10px",width:"100%"},onclick:()=>{STATE.bigOIndex++;STATE.bigOAnswered=false;render();}},STATE.bigOIndex+1>=STATE.bigOOrder.length?"See results":"Next question →"));}},opt);opts.appendChild(btn);});
  root.appendChild(opts);
};
function renderBigOResult(root){const total=STATE.bigOOrder.length;const score=STATE.bigOScore;const pct=Math.round((score/total)*100);root.appendChild(el("section",{class:"hero"},el("h2",{},"🎉 Quiz complete"),el("p",{},`You scored ${score} / ${total} (${pct}%)`)));const msg=pct>=80?"Excellent — you'd nail Q2.":pct>=50?"Solid — review the explanations and try again.":"Keep going — re-read Lectures 2 & 3 on complexity.";root.appendChild(el("p",{class:"muted",style:{textAlign:"center"}},msg));root.appendChild(el("button",{class:"btn",style:{width:"100%",marginTop:"12px"},onclick:()=>{STATE.bigOOrder=null;render();}},"Restart quiz"));}

SCREENS.sort=function(root){
  if(!STATE.sort){STATE.sort={algo:"insertion",values:SORT_PRESETS[0].values.slice(),input:SORT_PRESETS[0].values.join(", "),steps:null,stepIdx:0};buildSortSteps();}
  const s=STATE.sort;
  const algos=[{id:"insertion",label:"Insertion"},{id:"bubble",label:"Bubble"},{id:"selection",label:"Selection"},{id:"merge",label:"Merge"}];
  const chips=el("div",{class:"chip-row"});algos.forEach(a=>chips.appendChild(el("button",{class:"chip"+(s.algo===a.id?" active":""),onclick:()=>{s.algo=a.id;buildSortSteps();render();}},a.label)));root.appendChild(chips);
  const presetRow=el("div",{class:"chip-row"});SORT_PRESETS.forEach(p=>presetRow.appendChild(el("button",{class:"chip",onclick:()=>{s.values=p.values.slice();s.input=p.values.join(", ");buildSortSteps();render();}},p.name)));root.appendChild(presetRow);
  root.appendChild(el("section",{class:"card"},el("label",{class:"field"},"Array (comma separated):"),el("input",{type:"text",value:s.input,inputmode:"numeric",oninput:e=>{s.input=e.target.value;}}),el("div",{class:"btn-row",style:{marginTop:"10px"}},el("button",{class:"btn",onclick:()=>{const p=s.input.split(/[,\s]+/).map(x=>Number(x.trim())).filter(x=>!isNaN(x));if(!p.length){toast("Enter at least one number");return;}s.values=p.slice(0,12);s.input=s.values.join(", ");buildSortSteps();render();}},"Apply"),el("button",{class:"btn btn-secondary",onclick:()=>{s.values=Array.from({length:6},()=>Math.floor(Math.random()*90)+5);s.input=s.values.join(", ");buildSortSteps();render();}},"🎲 Random"))));
  const step=s.steps[s.stepIdx];const vizRow=el("div",{class:"array-row"});step.array.forEach((v,i)=>{let cls="arr-cell";if(step.sortedIdx&&step.sortedIdx.includes(i))cls+=" sorted";if(step.compareIdx&&step.compareIdx.includes(i))cls+=" compare";if(step.activeIdx&&step.activeIdx.includes(i))cls+=" active";vizRow.appendChild(el("div",{class:cls},String(v)));});root.appendChild(el("div",{class:"viz"},vizRow));
  root.appendChild(el("div",{class:"card"},el("h3",{},`Step ${s.stepIdx} / ${s.steps.length-1}`),el("p",{},step.desc)));
  root.appendChild(el("div",{class:"btn-row"},el("button",{class:"btn btn-secondary",onclick:()=>{s.stepIdx=Math.max(0,s.stepIdx-1);render();}},"← Step back"),el("button",{class:"btn",onclick:()=>{if(s.stepIdx<s.steps.length-1){s.stepIdx++;render();}else toast("Sorted!");}},"Step →"),el("button",{class:"btn btn-ghost",onclick:()=>{s.stepIdx=0;render();}},"⟲ Reset"),el("button",{class:"btn btn-ghost",onclick:()=>animateSort()},"▶ Play")));
  const cx=COMPLEXITY[s.algo];root.appendChild(el("section",{class:"card",style:{marginTop:"12px"}},el("h3",{},"Complexity of "+cx.name),el("p",{html:cx.html})));
};
function buildSortSteps(){const s=STATE.sort;s.steps=SORTERS[s.algo](s.values.slice());s.stepIdx=0;}
function animateSort(){const s=STATE.sort;if(s.timer){clearInterval(s.timer);s.timer=null;return;}s.timer=setInterval(()=>{if(s.stepIdx>=s.steps.length-1){clearInterval(s.timer);s.timer=null;return;}s.stepIdx++;render();},600);}
const SORTERS={
  insertion(A){const steps=[{array:A.slice(),desc:"Initial array. Insertion sort: maintain sorted prefix, insert each new element in place.",sortedIdx:[0]}];for(let i=1;i<A.length;i++){const key=A[i];steps.push({array:A.slice(),desc:`Take key = A[${i}] = ${key}. Compare backwards through the sorted prefix.`,activeIdx:[i],sortedIdx:Array.from({length:i},(_,k)=>k)});let j=i-1;while(j>=0&&A[j]>key){A[j+1]=A[j];steps.push({array:A.slice(),desc:`A[${j}] = ${A[j]} > ${key}, shift it right.`,compareIdx:[j,j+1],sortedIdx:Array.from({length:i+1},(_,k)=>k).filter(k=>k!==j+1)});j--;}A[j+1]=key;steps.push({array:A.slice(),desc:`Place key ${key} at position ${j+1}. Pass ${i} done — first ${i+1} elements sorted.`,sortedIdx:Array.from({length:i+1},(_,k)=>k)});}steps.push({array:A.slice(),desc:"✓ Array fully sorted.",sortedIdx:A.map((_,i)=>i)});return steps;},
  bubble(A){const steps=[{array:A.slice(),desc:"Initial array. Bubble sort: repeatedly compare adjacent pairs and swap if out of order."}];const n=A.length;for(let pass=0;pass<n-1;pass++){let swapped=false;for(let j=0;j<n-1-pass;j++){steps.push({array:A.slice(),desc:`Pass ${pass+1}: compare A[${j}]=${A[j]} and A[${j+1}]=${A[j+1]}.`,compareIdx:[j,j+1],sortedIdx:Array.from({length:pass},(_,k)=>n-1-k)});if(A[j]>A[j+1]){[A[j],A[j+1]]=[A[j+1],A[j]];swapped=true;steps.push({array:A.slice(),desc:`Swap — now A[${j}]=${A[j]}, A[${j+1}]=${A[j+1]}.`,activeIdx:[j,j+1],sortedIdx:Array.from({length:pass},(_,k)=>n-1-k)});}}steps.push({array:A.slice(),desc:`End of pass ${pass+1}. Largest unsorted element bubbled to position ${n-1-pass}.`,sortedIdx:Array.from({length:pass+1},(_,k)=>n-1-k)});if(!swapped)break;}steps.push({array:A.slice(),desc:"✓ Array fully sorted.",sortedIdx:A.map((_,i)=>i)});return steps;},
  selection(A){const steps=[{array:A.slice(),desc:"Initial. Selection sort: find min of unsorted suffix and swap into place."}];const n=A.length;for(let i=0;i<n-1;i++){let min=i;steps.push({array:A.slice(),desc:`Pass ${i+1}: assume A[${i}]=${A[i]} is the minimum.`,activeIdx:[i],sortedIdx:Array.from({length:i},(_,k)=>k)});for(let j=i+1;j<n;j++){steps.push({array:A.slice(),desc:`Compare A[${j}]=${A[j]} with current min A[${min}]=${A[min]}.`,compareIdx:[min,j],sortedIdx:Array.from({length:i},(_,k)=>k)});if(A[j]<A[min])min=j;}if(min!==i){[A[i],A[min]]=[A[min],A[i]];steps.push({array:A.slice(),desc:`Swap — smallest was A[${min}]. Now in position ${i}.`,activeIdx:[i,min],sortedIdx:Array.from({length:i+1},(_,k)=>k)});}else{steps.push({array:A.slice(),desc:`A[${i}] already smallest — no swap.`,activeIdx:[i],sortedIdx:Array.from({length:i+1},(_,k)=>k)});}}steps.push({array:A.slice(),desc:"✓ Array fully sorted.",sortedIdx:A.map((_,i)=>i)});return steps;},
  merge(A){const steps=[{array:A.slice(),desc:"Initial. Merge sort: divide in half recursively, merge sorted halves."}];function ms(arr,lo,hi){if(hi-lo<=1)return;const mid=Math.floor((lo+hi)/2);steps.push({array:A.slice(),desc:`Split A[${lo}..${hi-1}] at mid=${mid}.`,activeIdx:Array.from({length:hi-lo},(_,k)=>k+lo)});ms(arr,lo,mid);ms(arr,mid,hi);const L=arr.slice(lo,mid),R=arr.slice(mid,hi);let i=0,j=0,k=lo;while(i<L.length&&j<R.length){if(L[i]<=R[j])arr[k++]=L[i++];else arr[k++]=R[j++];}while(i<L.length)arr[k++]=L[i++];while(j<R.length)arr[k++]=R[j++];steps.push({array:arr.slice(),desc:`Merged A[${lo}..${hi-1}] into sorted order.`,sortedIdx:Array.from({length:hi-lo},(_,k)=>k+lo)});}ms(A,0,A.length);steps.push({array:A.slice(),desc:"✓ Array fully sorted.",sortedIdx:A.map((_,i)=>i)});return steps;}
};
const COMPLEXITY={insertion:{name:"Insertion sort",html:"<strong>Best:</strong> O(n) — already sorted, inner while never runs.<br><strong>Avg/Worst:</strong> O(n²).<br><strong>Space:</strong> O(1) in-place.<br><strong>Stable:</strong> yes."},bubble:{name:"Bubble sort",html:"<strong>Best:</strong> O(n) — no swaps on first pass.<br><strong>Avg/Worst:</strong> O(n²).<br><strong>Space:</strong> O(1) in-place.<br><strong>Stable:</strong> yes."},selection:{name:"Selection sort",html:"<strong>All cases:</strong> O(n²) — inner loop always runs.<br><strong>Space:</strong> O(1) in-place.<br><strong>Stable:</strong> typically no."},merge:{name:"Merge sort",html:"<strong>All cases:</strong> O(n log n) — T(n)=2T(n/2)+O(n).<br><strong>Space:</strong> O(n) auxiliary.<br><strong>Stable:</strong> yes."}};

SCREENS.bst=function(root){
  if(!STATE.bst){STATE.bst={values:BST_PRESETS[0].values.slice(),input:BST_PRESETS[0].values.join(", "),stepIdx:0,preset:BST_PRESETS[0].name,presetNote:BST_PRESETS[0].note};rebuildBST();}
  const s=STATE.bst;
  const presets=el("div",{class:"chip-row"});BST_PRESETS.forEach(p=>presets.appendChild(el("button",{class:"chip"+(s.preset===p.name?" active":""),onclick:()=>{s.preset=p.name;s.presetNote=p.note;s.values=p.values.slice();s.input=p.values.join(", ");rebuildBST();render();}},p.name)));root.appendChild(presets);
  root.appendChild(el("section",{class:"card"},el("label",{class:"field"},"Insert values in order (comma separated):"),el("input",{type:"text",value:s.input,inputmode:"numeric",oninput:e=>{s.input=e.target.value;}}),el("div",{class:"btn-row",style:{marginTop:"10px"}},el("button",{class:"btn",onclick:()=>{const p=s.input.split(/[,\s]+/).map(x=>Number(x.trim())).filter(x=>!isNaN(x));if(!p.length){toast("Enter at least one number");return;}s.values=p.slice(0,15);s.input=s.values.join(", ");s.preset="Custom";s.presetNote="Your own input.";rebuildBST();render();}},"Apply"),el("button",{class:"btn btn-secondary",onclick:()=>{const n=6+Math.floor(Math.random()*3);const used=new Set();const vals=[];while(vals.length<n){const v=Math.floor(Math.random()*90)+10;if(!used.has(v)){used.add(v);vals.push(v);}}s.values=vals;s.input=vals.join(", ");s.preset="Random";s.presetNote="Random practice set — try predicting balance first!";rebuildBST();render();}},"🎲 Random"),el("button",{class:"btn btn-ghost",onclick:()=>{s.stepIdx=Math.max(0,s.stepIdx-1);render();}},"← Back"),el("button",{class:"btn btn-ghost",onclick:()=>{s.stepIdx=Math.min(s.values.length,s.stepIdx+1);render();}},"Insert next →"))));
  if(s.presetNote)root.appendChild(el("p",{class:"muted",style:{padding:"0 4px"}},s.presetNote));
  const cur=s.stepIdx===0?"Empty tree — tap 'Insert next' to add "+s.values[0]+".":s.stepIdx<=s.values.length?`Inserted ${s.values[s.stepIdx-1]}. ${s.steps[s.stepIdx-1].note}`:"All values inserted.";
  root.appendChild(el("div",{class:"card"},el("h3",{},`Step ${s.stepIdx} / ${s.values.length}`),el("p",{},cur)));
  const tree=s.stepIdx===0?null:s.snapshots[s.stepIdx-1];
  root.appendChild(el("div",{class:"viz"},renderBSTSVG(tree,s.stepIdx>0?s.values[s.stepIdx-1]:null)));
  if(tree){const info=bstAnalyse(tree);root.appendChild(el("section",{class:"card"},el("h3",{},"Balance check"),el("p",{html:`<strong>Height:</strong> ${info.height}<br><strong>Nodes:</strong> ${info.size}<br><strong>Min balanced height:</strong> ${info.minH}<br><strong>Max |left−right| at any node:</strong> ${info.maxDiff}<br><br>`+(info.maxDiff<=1?`<span class="tag-pill green">BALANCED</span> — all subtree height diffs ≤ 1.`:`<span class="tag-pill red">UNBALANCED</span> — a node has subtree heights differing by ${info.maxDiff}.`)})));}
  if(tree){const inorder=[],preorder=[],postorder=[];(function tr(n){if(!n)return;preorder.push(n.key);tr(n.left);inorder.push(n.key);tr(n.right);postorder.push(n.key);})(tree);root.appendChild(el("section",{class:"card"},el("h3",{},"Traversals"),el("p",{html:`<strong>Inorder</strong> (L,N,R): ${inorder.join(" , ")}<br><em class="muted">Inorder of a BST is always sorted.</em><br><strong>Preorder</strong> (N,L,R): ${preorder.join(" , ")}<br><strong>Postorder</strong> (L,R,N): ${postorder.join(" , ")}`})));}
  if(s.stepIdx>0){const log=el("div",{class:"step-log"});s.steps.slice(0,s.stepIdx).forEach((st,i)=>log.appendChild(el("div",{class:"row"+(i===s.stepIdx-1?" current":"")},`${i+1}. `+st.note)));root.appendChild(log);}
};
function rebuildBST(){const s=STATE.bst;s.steps=[];s.snapshots=[];let root=null;s.values.forEach(v=>{const trace=[];root=bstInsert(root,v,trace);const path=trace.length===0?"as the root":"as "+trace.join(" → ");s.steps.push({value:v,note:`Placed ${v} ${path}.`});s.snapshots.push(cloneTree(root));});s.stepIdx=0;}
function bstInsert(node,key,trace){if(!node)return{key,left:null,right:null};if(key<node.key){trace.push(`left of ${node.key} (since ${key} < ${node.key})`);node.left=bstInsert(node.left,key,trace);}else if(key>node.key){trace.push(`right of ${node.key} (since ${key} > ${node.key})`);node.right=bstInsert(node.right,key,trace);}else{trace.push("(duplicate — ignored)");}return node;}
function cloneTree(n){if(!n)return null;return{key:n.key,left:cloneTree(n.left),right:cloneTree(n.right)};}
function bstAnalyse(root){let size=0,maxDiff=0;function rec(n){if(!n)return 0;size++;const l=rec(n.left),r=rec(n.right);maxDiff=Math.max(maxDiff,Math.abs(l-r));return 1+Math.max(l,r);}const height=rec(root);const minH=Math.ceil(Math.log2(size+1));return{size,height,maxDiff,minH};}
function renderBSTSVG(root,lastInserted){if(!root)return el("svg",{viewBox:"0 0 300 80"},el("text",{x:"150",y:"45","text-anchor":"middle",fill:"#94a3b8","font-size":"14"},"(empty tree)"));function depth(n){return n?1+Math.max(depth(n.left),depth(n.right)):0;}const H=depth(root);let x=0;function layout(n,d){if(!n)return;layout(n.left,d+1);n._x=x++;n._d=d;layout(n.right,d+1);}layout(root,0);const cols=x;const colW=Math.max(50,Math.min(80,Math.floor(360/Math.max(cols,1))));const rowH=60;const W=Math.max(300,cols*colW+30);const Ht=H*rowH+30;const nodes=[],edges=[];(function collect(n){if(!n)return;const cx=20+n._x*colW;const cy=20+n._d*rowH;nodes.push({key:n.key,cx,cy,last:n.key===lastInserted});if(n.left){collect(n.left);edges.push({x1:cx,y1:cy,x2:20+n.left._x*colW,y2:20+n.left._d*rowH});}if(n.right){collect(n.right);edges.push({x1:cx,y1:cy,x2:20+n.right._x*colW,y2:20+n.right._d*rowH});}})(root);const svg=el("svg",{viewBox:`0 0 ${W} ${Ht}`,style:{maxHeight:"420px"}});edges.forEach(e=>svg.appendChild(svgEl("line",{x1:e.x1,y1:e.y1,x2:e.x2,y2:e.y2,stroke:"#64748b","stroke-width":2})));nodes.forEach(n=>{const g=svgEl("g",{});g.appendChild(svgEl("circle",{cx:n.cx,cy:n.cy,r:19,fill:n.last?"#10b981":"#1e293b",stroke:n.last?"#34d399":"#94a3b8","stroke-width":n.last?3:2}));const t=svgEl("text",{x:n.cx,y:n.cy+4,"text-anchor":"middle",fill:n.last?"#03261b":"#e2e8f0","font-size":"13","font-weight":"700","font-family":"system-ui"});t.textContent=n.key;g.appendChild(t);svg.appendChild(g);});return svg;}
function svgEl(tag,attrs){const n=document.createElementNS("http://www.w3.org/2000/svg",tag);for(const[k,v]of Object.entries(attrs)){if(k==="style"&&typeof v==="object")Object.assign(n.style,v);else n.setAttribute(k,v);}return n;}

SCREENS.graph=function(root){
  if(!STATE.graph){const p=GRAPH_PRESETS[0];STATE.graph={preset:p.name,nodes:p.nodes.slice(),edges:p.edges.map(e=>e.slice()),start:p.start,directed:p.directed,algo:"bfs",steps:null,stepIdx:0,note:p.note};buildGraphSteps();}
  const g=STATE.graph;
  const algoRow=el("div",{class:"chip-row"});["bfs","dfs"].forEach(a=>algoRow.appendChild(el("button",{class:"chip"+(g.algo===a?" active":""),onclick:()=>{g.algo=a;buildGraphSteps();render();}},a==="bfs"?"BFS (Queue)":"DFS (Stack)")));root.appendChild(algoRow);
  const presetRow=el("div",{class:"chip-row"});GRAPH_PRESETS.forEach(p=>presetRow.appendChild(el("button",{class:"chip"+(g.preset===p.name?" active":""),onclick:()=>{g.preset=p.name;g.nodes=p.nodes.slice();g.edges=p.edges.map(e=>e.slice());g.start=p.start;g.directed=p.directed;g.note=p.note;buildGraphSteps();render();}},p.name)));root.appendChild(presetRow);
  root.appendChild(el("section",{class:"card"},el("div",{class:"row-between"},el("label",{class:"field",style:{margin:0}},"Start vertex: "),el("select",{onchange:e=>{g.start=e.target.value;buildGraphSteps();render();},style:{width:"100px"}},...g.nodes.map(n=>{const o=el("option",{value:n},n);if(n===g.start)o.selected=true;return o;}))),el("div",{class:"row-between",style:{marginTop:"8px"}},el("label",{class:"field",style:{margin:0}},"Directed: "),el("button",{class:"btn "+(g.directed?"":"btn-secondary"),onclick:()=>{g.directed=!g.directed;buildGraphSteps();render();}},g.directed?"Directed →":"Undirected ↔"))));
  if(g.note)root.appendChild(el("p",{class:"muted",style:{padding:"0 4px"}},g.note));
  root.appendChild(el("div",{class:"viz"},renderGraphSVG()));
  root.appendChild(el("div",{class:"graph-legend"},el("span",{},el("span",{class:"swatch",style:{background:"#10b981"}}),"current"),el("span",{},el("span",{class:"swatch",style:{background:"#38bdf8"}}),"visited"),el("span",{},el("span",{class:"swatch",style:{background:"#f59e0b"}}),"in queue/stack"),el("span",{},el("span",{class:"swatch",style:{background:"#1e293b",border:"1px solid #475569"}}),"unvisited")));
  const step=g.steps[g.stepIdx];
  root.appendChild(el("div",{class:"card"},el("h3",{},`Step ${g.stepIdx} / ${g.steps.length-1}`),el("p",{html:step.html})));
  const visited=g.steps.slice(0,g.stepIdx+1).map(st=>st.justVisited).filter(Boolean);
  root.appendChild(el("section",{class:"card"},el("h3",{},"Visit order so far"),el("p",{style:{fontFamily:"ui-monospace, monospace",fontSize:"16px"}},visited.join(" → ")||"(none)"),g.stepIdx===g.steps.length-1?el("p",{class:"tag-pill green"},"Complete!"):null));
  root.appendChild(el("div",{class:"btn-row"},el("button",{class:"btn btn-secondary",onclick:()=>{g.stepIdx=Math.max(0,g.stepIdx-1);render();}},"← Step back"),el("button",{class:"btn",onclick:()=>{if(g.stepIdx<g.steps.length-1){g.stepIdx++;render();}else toast("Traversal complete!");}},"Step →"),el("button",{class:"btn btn-ghost",onclick:()=>{g.stepIdx=0;render();}},"⟲ Reset"),el("button",{class:"btn btn-ghost",onclick:()=>animateGraph()},"▶ Play")));
  const log=el("div",{class:"step-log"});g.steps.forEach((st,i)=>log.appendChild(el("div",{class:"row"+(i===g.stepIdx?" current":"")},`${i}. `+st.text)));root.appendChild(log);
  root.appendChild(el("p",{class:"muted",style:{textAlign:"center",marginTop:"12px"}},"Tie-break rule: when multiple unvisited neighbours, pick the one earliest in the alphabet."));
};
function animateGraph(){const g=STATE.graph;if(g.timer){clearInterval(g.timer);g.timer=null;return;}g.timer=setInterval(()=>{if(g.stepIdx>=g.steps.length-1){clearInterval(g.timer);g.timer=null;return;}g.stepIdx++;render();},700);}
function adjacency(){const g=STATE.graph;const adj={};g.nodes.forEach(n=>adj[n]=new Set());g.edges.forEach(([u,v])=>{adj[u].add(v);if(!g.directed)adj[v].add(u);});const sorted={};g.nodes.forEach(n=>sorted[n]=[...adj[n]].sort());return sorted;}
function buildGraphSteps(){const g=STATE.graph;const adj=adjacency();const steps=[];if(!g.nodes.includes(g.start))g.start=g.nodes[0];if(g.algo==="bfs"){const visited=new Set([g.start]);const queue=[g.start];steps.push({visited:new Set(visited),frontier:queue.slice(),current:null,text:`Start: enqueue ${g.start}. Queue = [${g.start}].`,html:`Initialise with start vertex <strong>${g.start}</strong>. Queue = <code>[${g.start}]</code>.`});while(queue.length){const v=queue.shift();const newOnes=adj[v].filter(u=>!visited.has(u));newOnes.forEach(u=>{visited.add(u);queue.push(u);});steps.push({visited:new Set(visited),frontier:queue.slice(),current:v,justVisited:v,text:`Dequeue ${v}. ${newOnes.length?"Enqueue "+newOnes.join(", ")+" (alpha order).":"No new neighbours."} Queue = [${queue.join(", ")||"—"}].`,html:`Dequeue <strong>${v}</strong>. ${newOnes.length?"Visit & enqueue "+newOnes.map(x=>`<strong>${x}</strong>`).join(", ")+" (alphabetical).":"No unvisited neighbours."} Queue = <code>[${queue.join(", ")||"—"}]</code>.`});}steps.push({visited:new Set(visited),frontier:[],current:null,text:"✓ Queue empty — BFS complete.",html:`✓ BFS complete. Visit order: <strong>${steps.filter(s=>s.justVisited).map(s=>s.justVisited).join(" → ")}</strong>.`});}else{const visited=new Set();const stack=[g.start];steps.push({visited:new Set(visited),frontier:stack.slice(),current:null,text:`Start: push ${g.start}. Stack = [${g.start}].`,html:`Initialise with <strong>${g.start}</strong>. Stack = <code>[${g.start}]</code>.`});while(stack.length){const v=stack.pop();if(visited.has(v)){steps.push({visited:new Set(visited),frontier:stack.slice(),current:null,text:`Pop ${v} — already visited, skip.`,html:`Pop <strong>${v}</strong> — already visited, skip.`});continue;}visited.add(v);const newOnes=adj[v].filter(u=>!visited.has(u));const reversed=newOnes.slice().reverse();reversed.forEach(u=>stack.push(u));steps.push({visited:new Set(visited),frontier:stack.slice(),current:v,justVisited:v,text:`Pop ${v}, visit it. Push reversed: ${reversed.join(", ")||"none"}. Stack=[${stack.join(", ")||"—"}].`,html:`Pop & visit <strong>${v}</strong>. ${newOnes.length?"Push "+newOnes.map(x=>`<strong>${x}</strong>`).join(", ")+" — earliest letter popped first.":"No unvisited neighbours."} Stack=<code>[${stack.join(", ")||"—"}]</code>.`});}steps.push({visited:new Set(visited),frontier:[],current:null,text:"✓ Stack empty — DFS complete.",html:`✓ DFS complete. Visit order: <strong>${steps.filter(s=>s.justVisited).map(s=>s.justVisited).join(" → ")}</strong>.`});}g.steps=steps;g.stepIdx=0;}
function renderGraphSVG(){const g=STATE.graph;const W=320,H=280;const cx=W/2,cy=H/2;const R=Math.min(W,H)/2-30;const n=g.nodes.length;const pos={};g.nodes.forEach((node,i)=>{const angle=-Math.PI/2+(2*Math.PI*i)/n;pos[node]={x:cx+R*Math.cos(angle),y:cy+R*Math.sin(angle)};});const step=g.steps[g.stepIdx];const svg=el("svg",{viewBox:`0 0 ${W} ${H}`,style:{maxHeight:"320px"}});const defs=svgEl("defs",{});const marker=svgEl("marker",{id:"arr",viewBox:"0 0 10 10",refX:"10",refY:"5",markerWidth:"8",markerHeight:"8",orient:"auto"});marker.appendChild(svgEl("path",{d:"M0,0 L10,5 L0,10 z",fill:"#94a3b8"}));defs.appendChild(marker);svg.appendChild(defs);g.edges.forEach(([u,v])=>{const a=pos[u],b=pos[v];if(!a||!b)return;const dx=b.x-a.x,dy=b.y-a.y;const len=Math.hypot(dx,dy);const r=18;svg.appendChild(svgEl("line",{x1:a.x+(dx/len)*r,y1:a.y+(dy/len)*r,x2:b.x-(dx/len)*r,y2:b.y-(dy/len)*r,stroke:"#64748b","stroke-width":2,"marker-end":g.directed?"url(#arr)":""}));});g.nodes.forEach(node=>{const p=pos[node];let fill="#1e293b",stroke="#94a3b8",textColor="#e2e8f0",width=2;if(step.current===node){fill="#10b981";stroke="#34d399";textColor="#03261b";width=3;}else if(step.visited.has(node)){fill="#38bdf8";stroke="#0ea5e9";textColor="#022e44";}else if(step.frontier.includes(node)){fill="#f59e0b";stroke="#fbbf24";textColor="#3a1d00";}svg.appendChild(svgEl("circle",{cx:p.x,cy:p.y,r:18,fill,stroke,"stroke-width":width}));const t=svgEl("text",{x:p.x,y:p.y+5,"text-anchor":"middle",fill:textColor,"font-size":"14","font-weight":"700","font-family":"system-ui"});t.textContent=node;svg.appendChild(t);});return svg;}

render();
